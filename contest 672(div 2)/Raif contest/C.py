for i in range(int(input())):
    str = input()
    str = str.replace("AB","")
    str = str.replace("AB","")
    str = str.replace("BB","")
    if len(str)>0:
        print(len(str))
    else:
        print(0)
